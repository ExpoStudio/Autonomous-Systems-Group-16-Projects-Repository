This group project does involve some programming. 

I've considered that 1, we need a way to program or see code on the same file (so everyone's not operating independently); 
and 2, I'm not sure if y'all have the means to run the Coppelia Simulator or have Visual Studio Code/IDE with GitHub set up. 

So, this is our GitHub Repository. We will use this to track the changes members of the group made to the code here in the script. For the CopelliaSimulator, 
just copy and then paste the code into the script section of the Pioneer_p3dx object to test any changes.

If you are unfamiliar with github, There are things called threads. They are used to keep track of multiple versions of a document
I have created 5 additional threads -- one for each member of the team, and one where all 4 of us can collaborate on and edit at the same time. 
DO NOT directly make changes to the main thread. The main thread is our final/updated version of the script. If wanting to make independent changes or experiment, 
please use one of the other threads or create a new one. Request to merge changes onto main thread.
